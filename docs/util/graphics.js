/**
 *
 * @param {number} x
 * @param {number} y
 * @param {number} color
 */
function pset(x, y, color) {}

/**
 *
 * @param {number} x
 * @param {number} y
 * @returns {number}
 */
function pget(x, y) {
  return 0;
}

/**
 *
 * @param {number} x
 * @param {number} y
 * @param {string} pattern
 * @param {any} [collisionIndex]
 * @returns {any}
 */
function put(x, y, pattern, collisionIndex) {
  return {};
}

/**
 *
 * @param {number} x
 * @param {number} y
 * @param {string} text
 * @param {any} [collisionIndex]
 * @returns  {any}
 */
function print(x, y, text, collisionIndex) {
  return {};
}

/**
 * @type {any[]}
 */
let collision;

function initCollision() {}

function clearCollision() {}
